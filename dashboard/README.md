# Dashboard

Placeholder — decide between:

1. **QuickSight** — fastest path, connects directly to Athena, free tier
   covers a single author for portfolio purposes.
2. **Flask + React** — more visible in a portfolio (you control the UI),
   and doubles as React practice. Would read from Athena via boto3 on the
   backend and render charts in React on the frontend.

Once decided, this folder holds either the QuickSight dashboard export/
screenshots, or the Flask/React app source.
