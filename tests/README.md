# Tests

Placeholder for:
- Unit tests for `ingestion/lambda_function.py` (mock the API + S3 calls
  with `moto` or `unittest.mock`)
- A small local test for the Glue job transform logic using a sample JSON
  fixture, run outside of actual Glue (plain PySpark or pandas equivalent)
